apiVersion: v1
kind: Service
metadata:
  name: dev-deployment-service
spec:
  type: LoadBalancer
  selector:
    app: development-deployment  # Ensure this matches your deployment's label
  ports:
    - protocol: TCP
      port: 8080   # External port (Load Balancer)
      targetPort: 8080
